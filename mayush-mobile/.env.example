# Keep this equal to the Laravel APP_URL for the selected environment.
# Local Laragon example:
EXPO_PUBLIC_API_BASE_URL=http://mayush.test

# Optional, local-only credentials for scripts/visual-qa/*. Never expose these
# through EXPO_PUBLIC_* variables or commit real values.
FIGMA_FILE_KEY=wAdLNmlKanvI0AEPyEbrMs
FIGMA_ACCESS_TOKEN=
