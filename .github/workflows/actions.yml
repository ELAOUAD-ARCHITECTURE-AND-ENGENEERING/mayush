name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: 'just-a-placeholder-so-we-dont-get-errors'

      - name: Adding Known Hosts
        run: ssh-keyscan -H ${{ secrets.FTP_SERVER }} >> $HOME/.ssh/known_hosts

      - name: Deploy with rsync
        run: rsync -avz ./dist/ ${{ secrets.FTP_USER }}@${{ secrets.FTP_SERVER }}:/www/wwwroot/mayush.com/
     # steps:
    
    # - name: Checkout code
    #   uses: actions/checkout@v2

    # - name: Set up SSH
    #   run: |
    #     mkdir -p ~/.ssh
    #     echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    #     chmod 600 ~/.ssh/id_rsa
    #     ssh-keyscan -t rsa 89.117.48.26 >> ~/.ssh/known_hosts


    # - name: Deploy to server
    #   # with :
    #   #       server: ${{ secrets.FTP_SERVER }}
    #   #       username: ${{ secrets.FTP_USER }}
    #   #       password: ${{ secrets.FTP_PASSWORD }}
    #   #       key: ${{ secrets.MAYUSH DESIGN_SERVER }}
    #   #       protocol: ftps

    #   run: |
    #     # Replace the placeholders with your actual server details
    #     SERVER_HOST="89.117.48.26"
    #     SERVER_USERNAME="root"
    #     SERVER_PATH="/www/wwwroot/mayush.com"

    #     # Create the target directory on the server
    #     #ssh $SERVER_USERNAME@$SERVER_HOST "mkdir -p $SERVER_PATH"

    #     # Transfer all files to the server using rsync
    #     rsync -avz --delete ./ $SERVER_USERNAME@$SERVER_HOST:$SERVER_PATH/

    #     echo "Deployment successful!" 
